# DOSSIER full experiment environment.
#
# Target runtime:
# - Python 3.11
# - CUDA-capable Linux server for local vLLM serving
# - vLLM server version observed in the experiment logs: 0.19.0
#
# Install:
#   python3.11 -m venv .venv
#   . .venv/bin/activate
#   python -m pip install --upgrade pip setuptools wheel
#   python -m pip install -r requirements.txt
#   python -m pip install -e .

# Core DOSSIER client/runtime dependencies
openai==2.32.0
python-dotenv==1.2.2
httpx==0.28.1
httpcore==1.0.9
anyio==4.13.0
sniffio==1.3.1
jiter==0.14.0
pydantic==2.13.3
pydantic-core==2.41.4
typing-extensions==4.15.0
tqdm==4.67.3
requests==2.33.1

# Optional Gemini backend used by backend.GeminiClient
google-generativeai==0.7.1
google-ai-generativelanguage==0.6.6
google-api-core==2.30.0
google-auth==2.49.1
protobuf==6.33.6

# Loong / data tooling and tokenizer utilities
numpy==2.4.4
pandas==3.0.2
tiktoken==0.7.0
sentencepiece==0.2.0
PyYAML==6.0.1

# Local model / vLLM serving stack
vllm==0.19.0
transformers==4.57.6
tokenizers==0.22.2
accelerate==1.13.0
safetensors==0.4.3
huggingface-hub==0.36.2

# vLLM 0.19.0 installs the matching torch stack through its own dependencies.
# Keep torch unpinned here to avoid CUDA wheel conflicts across servers.
# If your server requires a specific CUDA wheel, install torch first using the
# PyTorch index for that CUDA version, then run this requirements file.
